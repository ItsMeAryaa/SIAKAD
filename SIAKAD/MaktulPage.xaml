<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="SIAKAD.MaktulPage"
    Title="Mata Kuliah">

    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="tambah_matkul" Clicked="tambahMatkul" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout>

            <ListView
                x:Name="ListMK"
                HasUnevenRows="True"
                SeparatorVisibility="None"
                ItemSelected="listMatkul">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>

                            <Frame
                                Margin="10.5"
                                Padding="10"
                                CornerRadius="10"
                                HasShadow="True"
                                BackgroundColor="White"
                            >

                                <StackLayout
                                    Orientation="Horizontal"
                                    Spacing="10">

                                    <StackLayout VerticalOptions="CenterAndExpand">
                                        <Label
                                            Text="{Binding Nama}"
                                            FontSize="Medium"
                                            FontAttributes="Bold"
                                            TextColor="Black" />

                                        <Label
                                            Text="{Binding Kode}"
                                            FontSize="Small"
                                            TextColor="Gray" />

                                        <Label
                                            Text="{Binding Sks}"
                                            FontSize="Small"
                                            TextColor="Gray" />
                                    </StackLayout>

                                </StackLayout>

                            </Frame>

                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>

        </StackLayout>
    </ContentPage.Content>

</ContentPage>